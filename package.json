{
  "name": "nityaparmar-jobsphere",
  "version": "1.0.0",
  "private": true,
  "description": "JobSphere — Enterprise Career Portal & ATS",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "client": "npm --prefix client run dev",
    "server": "npm --prefix server run dev"
  }
}
